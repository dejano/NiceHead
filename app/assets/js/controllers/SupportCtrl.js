(function (angular, undefined) {
    'use strict';
    
    angular.module("company").controller("SupportCtrl", MainController);

    function MainController($scope, $location, $interval) {

    }
}(angular));