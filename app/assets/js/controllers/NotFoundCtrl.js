(function (angular, undefined) {
    'use strict';
    
    angular.module("company").controller("NotFoundCtrl", MainController);

    function MainController($scope, $location, $interval) {

    }
}(angular));